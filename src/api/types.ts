export type Life = {
  id: string
  triggerTime: string
  triggerNumber: number
}
export type Money = {
  id: string
  amount: number
  recordDate: string
  category: string
  notes: string
}
